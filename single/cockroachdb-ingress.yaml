---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: cockroachdb
  annotations:
spec:
  rules:
    - host: cockroachdb.rancher
      http:
        paths:
          - backend:
              serviceName: cockroachdb
              servicePort: 8080
